
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Projetos nativos: managed workflow → o EAS faz prebuild a partir do app.json.
# Mantidos só LOCALMENTE (para `expo run:android`/`run:ios`), nunca versionados —
# versioná-los faz o EAS compilar o nativo (Gradle) em vez do prebuild gerenciado.
/android
/ios